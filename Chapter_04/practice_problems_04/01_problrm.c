// Write a program to print natural numbers from 10 to 20 when initial loop counter is initialize to 0.

#include <stdio.h>

int main()
{
    for (int i = 0; i <= 10; i++)
    {
        printf("%d\n", i + 10);
    }

    return 0;
}