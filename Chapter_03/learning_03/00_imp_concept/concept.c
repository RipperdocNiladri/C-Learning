// In C, 0 means false, while any non-zero value means true.
#include <stdio.h>

int main()
{
    if (0)
    {
        printf("false");
    }
    if (5)
    {
        printf("true");
    }
    return 0;
}